import React from 'react';
import useTitle from '../../hook/useTitle';

const Blog = () => {
    useTitle('Blog')
    return (
        <div>
            
        </div>
    );
};

export default Blog;